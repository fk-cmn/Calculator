# Calculator

This repository will contain a Visual Studio 2022 MFC dialog-based calculator project (Calculator). I will add a feature branch `calculator-mfc` with the full project including support for expression precedence, screenshots, and the lab report.

Temporary initial commit to allow creating branches. The project files will be added to the `calculator-mfc` branch shortly.
